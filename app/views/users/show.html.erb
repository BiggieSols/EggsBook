<h1><%= @user.email %></h1>

<ul>
  <% @user.attributes.each do |attr| %>
    <li>
      <%= attr[0] %>: <%= attr[1] %>
    </li>
  <% end %>
</ul>

<% @user.favorite_foods.each do |food| %>
  <div><%= food.name %></div>
<% end %>